# 2026-07-08 工作日志

## 一、制定 Clarity 设计系统（第一阶段）
为测试用例显示工具制定全新前端设计系统「Clarity · 清晰」：
- `app/static/css/design-tokens.css` — 设计令牌层：Slate 冷灰中性色 + Indigo 主色（#4f46e5），明暗双主题，8 点栅格，分层阴影，三层令牌架构（色阶→语义→组件）
- `docs/design-system.md` — 完整设计方案文档：色彩/字体/间距/组件/响应式/可访问性/框架选型/落地路径
- `docs/design-preview.html` — 可视化预览页：色板/字体/间距/圆角阴影/全部组件 + 用例卡片实际效果，含暗色切换

设计决策：方案 A 优先（原生 CSS + tokens，无构建依赖），契合项目「双击即用」部署诉求。

## 二、Clarity 落地实施（第二阶段，用户认可效果后）
通读 `app.js` 发现关键约束：JS 内联样式引用旧变量名（--surface/--primary/--bg 等），且硬编码 #4f46e5/#16a34a 恰与新主色一致。确立「零 JS 改动 + 兼容别名层 + class 名不变」三铁律。

改动 3 个文件：
- `design-tokens.css` — 追加兼容别名层，旧变量名→新语义令牌映射
- `style.css` — 重写：删除旧 :root 与 dark 媒体查询，全部样式值令牌化，视觉对齐预览页
- `index.html` — 引入 design-tokens.css，版本号 v3→v4 避缓存
- `docs/design-migration-plan.md` — 落地方案文档：策略/别名映射表/class 改造对照/风险回滚/验证清单

`app.js` 零改动。

## 三、发现既有遗留（非本次引入）
`app.js` switchView 第 38 行引用 `tabSearch`（HTML 中已移除），会抛 null 错误。已记入方案文档第八节，建议下迭代加 if 守卫。

## 四、运行验证（已通过）
重启实例后验证：
- 首页正确加载 design-tokens.css?v=1 + style.css?v=4
- API /api/init 正常（loaded=True, total=63）
- 用例卡片渲染正常（27 字段）
- style.css 含 124 处 --color-* 令牌引用，0 处旧主色 #2563eb
- design-tokens.css 可访问（HTTP 200, 17592 bytes）

关键坑：Flask debug=off 时模板不热重载，改 index.html 模板必须重启进程才生效（静态 CSS 则动态读取）。
应用现运行于 http://127.0.0.1:8765
