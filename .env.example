# 测试用例显示工具 - 环境变量配置
# 使用方法：复制本文件为 .env，按需修改变量值
#   cp .env.example .env

# 服务端口（默认 8765，与原 testcase_viewer.py 一致）
PORT=8765

# 数据目录（存放 Excel 测试用例文件）
DATA_DIR=testcases

# Flask 调试模式（1 开启，0 关闭）
FLASK_DEBUG=0
